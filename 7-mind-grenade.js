const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is: ${num1 + num2}`);
}

addValues();
// sin usar export, al llamar a este módulo con require('./7-mind-grenade')
// la función addValues() se ejecutará automáticamente.
